apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pds-client
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/<you>/nhs-interop-platform
    targetRevision: HEAD
    path: k8s/helm/pds-client
    helm:
      releaseName: pds-client
  destination:
    server: https://kubernetes.default.svc
    namespace: interop
  syncPolicy:
    automated: { prune: true, selfHeal: true }
    syncOptions: [CreateNamespace=true]
